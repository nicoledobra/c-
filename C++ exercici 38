#include <iostream>

using namespace std;

int main()
{
    int a, b, c, d;
    cin >> a;
    cin >> b;
    c=a*b;
    d=a * -1;
    cout << "Results of the calculation are " << c << " and " << d;
    return 0;
}
